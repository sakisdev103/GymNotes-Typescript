import { DateFilter, Header, Workout } from "@/components";

const Home = () => {
  return (
    <div>
      <Header />
      <DateFilter />
      <Workout />
    </div>
  );
};
export default Home;
